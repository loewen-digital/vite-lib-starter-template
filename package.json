{
  "name": "vite-lib-starter-template",
  "version": "0.0.0",
  "private": true,
  "description": "Starter for loewen-digital libraries: Vite library mode, TypeScript, Vitest, ESM only.",
  "type": "module",
  "scripts": {
    "check": "echo \"check: not set up yet (see issue #1)\"",
    "test": "echo \"test: not set up yet (see issue #1)\"",
    "build": "echo \"build: not set up yet (see issue #1)\""
  },
  "engines": {
    "node": ">=22"
  }
}
